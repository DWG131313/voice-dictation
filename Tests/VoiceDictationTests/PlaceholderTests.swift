import XCTest

final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        // This file exists to suppress the "no source files" warning.
        // It will be replaced by real tests.
        XCTAssertTrue(true)
    }
}
